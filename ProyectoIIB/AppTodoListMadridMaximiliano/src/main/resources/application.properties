# Configuración de prueba mejorada
spring.datasource.url=jdbc:postgresql://todolistdb.postgres.database.azure.com:5432/postgres?sslmode=require
spring.datasource.username=dbadmin
spring.datasource.password=PgSQL#SprintF1n@l
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Mostrar SQL en consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Inicialización de datos
spring.sql.init.mode=never
spring.sql.init.continue-on-error=true

# Deshabilitar Flyway para tests
spring.flyway.enabled=false
